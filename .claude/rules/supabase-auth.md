---
paths:
  - lib/**
---

# Supabase & auth logic

- Invoke `supabase:supabase` for Supabase work; add `vercel:routing-middleware` when changing proxy/matcher behavior.
- Supabase is auth-only: no app tables; local files before any schema.
- File-level detail: `.claude/references/lib.md`.
