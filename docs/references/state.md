# Where things stand

Read this first in any new session. Written September 23, 2026, at the end of a long session that the owner intends to rewind to the "Oparax, start to finish" explanation of September 22 and continue from. Everything agreed, changed or left open after that explanation is here, so nothing has to be re-argued. The one-line record of every decision is [decisions.md](decisions.md); this file is the handoff: what was done, what is pending, what the owner still finds unclear, and the order of the next steps.

The owner's standing instruction for every explanation: plain product terms, reasons with every recommendation, no em dashes, nothing invented and presented as his decision, and whole-picture answers rather than literal answers to one sentence.

## 1. Done since the September 22 explanation

1. **Review lanes** (commit 37216a0, pushed). Feature critique runs eight runner lanes plus a Claude Opus subagent lane; QC runs nine plus Opus. The three new runner lanes are Kimi K3, GLM 5.2 and Muse Spark 1.3 through `cursor-agent` on the owner's Cursor Pro+ pool (read-only plan mode, sandbox forced on, workspace trusted; the runner has a `cursor` provider and one resume). The Opus lane runs only when Claude Code hosts the stage. Each lane stays only if it keeps finding things the others miss. Best of N in the review sense is what this is; Cursor's own editor-only `/best-of-n` was not wired.
2. **Design skills, phase 0A** (this commit). Installed for Claude and Codex under `~/.agents/skills` with symlinks: `beautiful-shadows`, `accessibility`, `emil-design-eng`, `design-review`. Removed: `framer-motion-animator` (unused) and the project-only `ui-ux-pro-max` (Codex could not read it). Kept: `frontend-design`, `web-design-guidelines`, `ai-elements`, `vercel:shadcn` (it applies presets, so no second shadcn skill), `vercel:react-best-practices`. `email-html-mjml` is installed when sign-up (slice 6) is planned, for the sign-up and password emails. The ui bundle in `.claude/skills/feature/SKILL.md` step 3 reflects this.
3. **Every ui skill has three jobs** (owner, September 23): it shapes the plan, guides the build, and checks the result. A skill loaded only at build time makes the build guess choices the plan never wrote down. The bundle table says so; the design brief (below) carries the planning decisions.
4. **Design moves to Claude Design** (owner, September 23). `/feature` step 1.1 no longer generates a design; it writes a design brief, the owner designs in Claude Design on the web, and the export becomes the plan's visual contract. `references/design-exploration.md` is retired.
5. **Decisions ledger** ([decisions.md](decisions.md)) and its pointer in AGENTS.md are committed.
6. **Verified facts the next session can rely on:** Cursor is logged in; `agent` on this machine is Grok's CLI and `cursor-agent` is Cursor's; `agent-browser` is installed; Vercel is Pro (cron every minute, functions to 800 seconds); the Supabase database holds no product data but still has two unused Stripe columns from the retired #131 branch; X timeline polling with `exclude=replies,retweets` bills only originals and quotes, 10,000 requests per 15 minutes, deduplicated within a UTC day.

## 2. Agreed in principle, not yet applied (each needs the owner's explicit yes, then one meta commit)

1. **Design system reset, its own small issue before slice 1.** The custom dark system in DESIGN.md mostly describes the retired drafting product. Direction (owner, September 23): simplify to stock shadcn with light and dark, dark by default, keep the logo, keep one accent. Procedure: load `frontend-design`, `accessibility`, `beautiful-shadows` and `vercel:shadcn` and decide each shadcn create choice with a reason from those skills (style, base color, theme color, chart color, fonts, radius); the owner previews on ui.shadcn.com/create and copies the preset code; the build applies it (`npx shadcn@latest init --preset <code>`), sets the fonts, adds the theme toggle, shrinks DESIGN.md to the decisions and their reasons; then the owner runs `/design-sync` once so Claude Design has the system. The repo is the source of truth; Claude Design is a copy; when the look changes, re-apply and re-sync. Open inside this: the fonts (Hanken Grotesk was a deliberate choice; `frontend-design` says not to reach for a stock default such as Geist; one text family plus one mono is the target), and whether the light toggle is shown. The owner's earlier shadcn create choices were Mira, Zinc, Blue, Cyan, Nunito Sans and IBM Plex Sans; only the fonts are in question.
2. **Reviewers read the slice's skill files.** The stage copies the slice's SKILL.md files into `.feature/lanes/skills/` and the brief tells every lane to read them before judging; no connectors or MCPs for reviewers. This is the fair-critique fix: today reviewers get only a line of skill names as "rules to weigh".
3. **Screenshot check in QC** (owner, September 23: "an agent browser can simply take a screenshot... that's a pretty small check"). After the gates, one agent starts the app off-screen on a free port, screenshots the slice's pages at desktop and phone width in dark and light with `agent-browser`, stops the app; the QC session reviews the screenshots with `design-review` and `accessibility` against the Claude Design export; findings join the fix list. Pages show a saved example monitor so no paid onboarding runs. Needs the owner's word because "no stage runs the app" gets this one exception.
4. **No browser-walking verify step** (owner, September 23, after the retirement reasons were laid out). Vercel's `verification` skill is not a build step; at most a tracing checklist for bug fixes.
5. **Engineering principles.** A section of about 25 lines in AGENTS.md (planning: compare before choosing, model the domain first, redesign over bolt-on, name the proof; building: laziness, subtract first, low reader load, check outside data at the door, safe to repeat, root causes; hygiene: comments say why, no slop, no slop words) plus a "principles" lens that every reviewer grades against in plan critique and QC. Today no principle is stated anywhere except one build line, and QC does not grade even that. The draft text is in the September 23 chat; the "about 400 lines" file limit in it is the assistant's number.
6. **Tests and alerts.** A few unit tests on pure functions where a silent mistake costs money, security or the algorithm's rules (the X handle format, allowed return paths after login, "an unknown cost stays unknown", the Jev cutoff lines, the quote check on cards), run by build's existing gates with Node's built-in test runner; one PostHog alert ("error issue created or reopened") to a Slack channel; the rule that every bug fix adds one test that reproduces the bug. No end-to-end browser suites until the pages stop changing.
7. **Docs consolidation.** Eighteen files to seven plus a folder: roadmap.md, onboarding-algorithm.md, downstream-algorithm.md (absorbing the two lab reports' conclusions), references/cogs.md, references/decisions.md (absorbing decisions-2026-09-22.md), one setup.md (merging setup-status, setup-env, setup-x, setup-smtp), DESIGN.md; and docs/discovery/ for findings.md, reshad.md, people.tsv, exp1.md. Everything stays in git history.
8. **Narrow the slices** (owner's narration, September 22). #133 becomes "onboarding: type a handle and a sentence, watch the build happen live, land on the page with its sources and an empty feed section"; the onboarding page and the feed page are one address in two states. #134 is single-source cards only (grouping moves to its own later slice, per the two-views decision). Tabled until the owner brings them back: X Ads, GitHub, Product Hunt, grouped stories, watching X accounts. Supabase, Vercel and PostHog are not tasks; each slice's plan names what it touches.

## 3. Open rulings, by when they are needed

1. **Before slice 1:** the yeses in section 2 (items 1, 2, 3, 5, 6, 7, 8); the fonts and the preset; the PostHog Slack channel (the owner's click, or "set it up").
2. **Before slice 2:** the article fit line (keep 0.75 on and 0.35 off, lower it, or count the unsure band as on: the lab put 8 of Nihan's 25 items in that band, several plainly relevant); what Jev is told about the person (beat only, plus Grok's paragraph, plus the person's posts at about 25 cents a person a month); which writer (Qwen 3.7 Flash copies quotes most cleanly, Ling produced more cards at no cost; the lab rates are automated acceptance, not truth); what a new monitor shows on day zero.
3. **In their slices:** where polling runs and how often (slice 3; Vercel Pro cron every minute is the assistant's recommendation); how watched X accounts arrive (slice 5; polling looks best, three assistant recommendations exist and none is a decision); Stripe direct account or marketplace, and the plugin install (slice 7); the price (slice 7, no price is decided); email and Slack as alert channels (after the slices, owner's lean); the bot handle (the owner creates the plain account under farzan@oparax.ai and tries for @oparax before slice 4).

## 4. What the owner has said he does not understand, and how to answer it

1. **The onboarding algorithm.** He has read it several times and still does not hold it. The plan: he watches it run live in slice 1, step by step, with each step's input and output shown on the building page. Until then, explain it as four steps with what goes in and comes out, never as prompts (the verbatim prompts are in onboarding-algorithm.md).
2. **The downstream algorithm.** Discuss it one step at a time in chat, before slice 2, and write each ruling into downstream-algorithm.md with his name and date. Every R-numbered rule there is the assistant's proposal until it carries his date.
3. **Agent or pipeline.** Onboarding step 3 is a real tool-loop agent (Grok, six-step cap, two tools). The downstream is a fixed pipeline, and should be. "Every desk is an agent" is a product sentence, not a design; in this repo "agent" already means one monitor row. eve was installed June 17 and removed July 13 because it could not deploy; it is not coming back.
4. **Design surfaces.** There are two, not four: the repo (where the look lives, applied from a shadcn preset code) and Claude Design (a synced copy to design with). The Claude app's Settings, Design systems, is the same list as Claude Design's. ui.shadcn.com/create is only the picker that produces the preset code.
5. **Which doc is for what.** Answered by the consolidation in section 2, item 7.
6. **Testing, verification and PostHog together.** One loop: build gates plus a few unit tests before ship; PostHog records errors while running; one alert reaches Slack; a fix goes through the bug-fix flow and leaves a test behind. Vercel's verification skill and cstack's verification generators are not part of it.
7. **The skills and where they act.** The three-jobs rule (section 1, item 3) and the ui bundle table are the answer. cstack and pstack were analyzed on September 23 and not installed: their principles become the AGENTS.md section, their verification generators repeat the retired verify step, `poteto-mode` is a rival flow.
8. **His own pattern.** He has said he over-builds meta tooling; the flow has been rewritten at least eight times since February. After the items in section 2, the flow is frozen until slice 1 ships.

## 5. The order from here

1. The owner rules on section 2 and sends the preset code; one meta commit applies the flow edits (items 2, 3, 5), the tests-and-alerts rule (6) and the docs consolidation (7); AGENTS.md is updated to match.
2. The design-system reset issue: apply the preset, fonts, toggle, shrink DESIGN.md; then `/design-sync`.
3. `/feature 133` (narrowed to onboarding) in a fresh chat: the talk-through, the design brief, Claude Design, the plan, critique, build, QC with the screenshot check, the owner's walk, ship.
4. Slice 2 (single-source cards) after the three downstream rulings; slice 3 (keeping it moving); then the landing page, sign-up, the bot, payment, one at a time.
