# Where things stand

Read this first in any new session, before the roadmap. Written September 23, 2026, at the end of a long session that the owner is rewinding to the "Oparax, start to finish" explanation of September 22 and continuing from. Everything agreed, changed or left open after that explanation is here, so nothing has to be re-argued and nothing he already decided gets re-asked. The one-line record of every decision is [decisions.md](decisions.md); this file is the handoff: what was done, what is pending, what the owner still finds unclear, and the order of the next steps.

The owner's standing instruction for every explanation: plain product terms, reasons with every recommendation, no em dashes, nothing invented and presented as his decision, whole-picture answers rather than literal answers to one sentence, and no new documents for him to read (files are for the agents; he reads chat).

September 24, late: production is live again (owner). `main` matches `beta` through promote commit 7955624, Vercel production was unpaused, and `https://oparax.ai` serves the placeholder homepage with `/privacy` and `/terms` for Google's branding review; the owner updates the Google and X links and requests re-verification.

September 24 account-setup update: use `../setup.md` for current external-service facts, overriding the older snapshots below. Google and X OAuth 2.0 are enabled in Supabase; Google's Oparax client is External and In production. GitHub public-read access (no expiration), Product Hunt, the X bot, PostHog source-map upload credentials, and Stripe sandbox keys are installed in Vercel and pulled locally. All seventeen variables have exactly one readable Config entry each, with all three environments selected together, at the owner's explicit direction. The total includes Product Hunt's application key and secret added in the follow-up, alongside its developer token. `.env.bot.local` and stale local-only keys are removed after verification. Domains are retained, Railway shows an expired trial, and production remains paused. Website sign-up, bot delivery, Stripe webhooks and automatic per-build source-map upload are still feature work. No request was sent to Claude.

September 24 branding follow-up: Google reported six verification issues, recorded with remedies in [setup.md](../setup.md#google-branding-verification-follow-up): unreachable homepage, unverified domain ownership, missing homepage privacy link, unreachable privacy page, insufficient privacy content, and identical homepage/privacy URLs. No logo-specific rejection was listed. **Domain ownership is now complete:** Search Console returned Ownership verified for the `oparax.ai` Domain property under `farzanmrz@gmail.com`, after adding the new root TXT record in GoDaddy. Preserve that record. Website/email DNS records and production pause were unchanged. Publish proper pages and replace the temporary links before resubmitting branding; the user-facing support email should become `farzan@oparax.ai` when available. These remaining items are pending. The Stripe connector here lists the matching Oparax sandbox in test mode and exposes read/write tools; a complete permission grant and Claude's connection were not independently verified.

If the app's conversation rewind has undone files in the working tree, restore them first: `git status --short`, then `git checkout -- .` puts back what is committed. Everything below is committed and pushed on `beta`.

## 1. Done and committed (nothing here needs re-doing)

1. **Review lanes** (commit 37216a0). Feature critique runs eight runner lanes plus a Claude Opus subagent lane; QC runs nine plus Opus. The three new runner lanes are Kimi K3, GLM 5.2 and Muse Spark 1.3 through `cursor-agent` on the owner's Cursor Pro+ pool (read-only plan mode, sandbox forced on, workspace trusted; the global runner has a `cursor` provider and one resume). Tested end to end, and all eight runner lanes re-verified on September 24 on a smoke brief (the runner now accepts a findings array wrapped in prose, which Grok and Muse Spark do). The Opus lane runs only when Claude Code hosts the stage. Each lane stays only if it keeps finding things the others miss. Best of N in the review sense is what this is; Cursor's editor-only `/best-of-n` was not wired.
2. **Design skills, phase 0A** (commit a0da26e). Installed for Claude and Codex under `~/.agents/skills` with symlinks: `beautiful-shadows`, `accessibility`, `emil-design-eng`, `design-review`. Removed: `framer-motion-animator` (unused) and the project-only `ui-ux-pro-max` (Codex could not read it). Kept: `frontend-design`, `web-design-guidelines`, `ai-elements`, `vercel:shadcn` (it applies presets, so no second shadcn skill), `vercel:react-best-practices`. `email-html-mjml` is installed when sign-up (slice 6) is planned, for the sign-up and password emails. The ui bundle in `.claude/skills/feature/SKILL.md` step 3 says which skill does what.
3. **Every ui skill has three jobs** (owner): it shapes the plan, guides the build, and checks the result. A skill loaded only at build time makes the build guess choices the plan never wrote down.
4. **Design moves to Claude Design** (owner). `/feature` step 1.1 writes a design brief instead of generating a design; the owner designs on the web; the export is the plan's visual contract. The generation path is retired.
5. **Reviewers read the slice's skill files** (this commit). Feature critique and QC copy the plan's skills into `.feature/lanes/skills/` and every lane reads them before judging. No connectors or MCPs for reviewers.
6. **The screenshot check in QC** (this commit, owner's word). QC step 4a: one agent starts the app off-screen on a free port, screenshots each page the plan names at 1280px and 390px in dark and light with `agent-browser`, stops the app; the QC session reviews the images with `design-review` and `accessibility`. No clicking, no sign-in, no paid calls. AGENTS.md carries the exception.
7. **Engineering principles** (this commit). A section in AGENTS.md, read by both harnesses at every stage, graded by a "principles" lens in plan critique and QC. The wording is the assistant's draft; the owner may change any line. cstack and pstack were analyzed (13 agents) and not installed; their principles became this section, their verification generators repeat the retired verify step, `poteto-mode` is a rival flow.
8. **Five issues, the owner's structure** (September 23). 1 onboarding: understand the algorithm and build the onboarding page (#143); 2 the feed page and the downstream algorithm, single-source cards, with the feed keeping itself moving (#144); 3 the landing page and the whole flow (#145); 4 sign-up on the feed page (#146); 5 after sign-up: editing sources, alerts with cadence and channel, watched X accounts, payment (#147). Tabled: #136 GitHub and Product Hunt, the grouped view, ads. Rewritten fresh on September 24 as #143 to #147 (owner: create the issues anew in the correct order); the older issues are closed and point at them. The roadmap's section 13 matches.
9. **Docs consolidated** (this commit). `docs/` is now: roadmap.md, onboarding-algorithm.md, downstream-algorithm.md, source-table-seed.json, one setup.md (the four setup files merged), references/ (cogs.md, decisions.md, this file, the two September 21 lab reports), discovery/ (findings.md, reshad.md, people.tsv, exp1.md). The September 22 decisions note is folded into decisions.md; git keeps every removed file.
10. **Verified facts the next session can rely on:** Cursor is logged in; `agent` on this machine is Grok's CLI and `cursor-agent` is Cursor's; `agent-browser` is installed; Vercel is Pro (cron every minute, functions to 800 seconds); the earlier claim that the Supabase database held no product data was incorrect. #148 removed 69,993 legacy rows and every application object from `public`; Supabase Auth's 11 users are untouched. Stripe was attempted once on August 28 through the Vercel marketplace and stalled at a browser terms step, nothing else exists; X timeline polling with `exclude=replies,retweets` bills only originals and quotes, 10,000 requests per 15 minutes, deduplicated within a UTC day; eve was installed June 17 and removed July 13 because it could not deploy.
11. **Clear the ground (#148), shipped to `beta` on September 24 (commit 3cc4d5e).** Planned with Astra in the pair protocol, critiqued by eight lanes plus Opus, adjudicated, built on Astra, QC round 1 (nine lanes plus Opus, the screenshot pass, six small fixes applied), then squashed onto `beta`; the worktree it was built in and the `ft/148` branch were deleted at the owner's word; the tag `archive/legacy-drafting` holds the old tree. One correction to what the owner was told: the research inventory said every product table was empty; the teardown found and deleted 69,993 rows of old drafting data (posts, model calls and the like), which the owner's ruling covered but which the inventory misreported. The legacy drafting code, workers, 70 local migration files and the live public application schema are removed. The live teardown `20260925001511_teardown_legacy_schema` left no application relations, routines, types, policies or non-internal triggers, while preserving `citext` and Supabase Auth. `archive/legacy-drafting` preserves the pre-deletion commit. The landing keeps `landing_cta_clicked` and `signed_out` as its named events; the anonymous `PostHogUserContext` clears a recovery visitor's old identity on the login page. Two QC findings were deliberately left for later issues: the hero heading is still bold against DESIGN.md's no-bold default (issue 3 rules on the landing page), and the first pageview on an auth arrival can still carry the previous identity before the reset effect runs (issue 4 redesigns auth).
12. **The counsel skill** (September 24, owner: "create a skill called counsel"). `/counsel` and `$counsel`, user-invoked only in both harnesses, at `~/.agents/skills/counsel` with the Claude wrapper at `~/.claude/skills/counsel` and a Codex link. Five default advisers through their own CLIs (Astra, Gemini Pro, Grok, Opus, Fable), all high; `only`, `add` and exact model overrides; Cursor's Kimi, GLM and Muse, Sol, Terra, Flash and Sonnet by name. The shared critique runner gained a `claude` provider (plan mode, read-only tools, no MCP servers, no skills, so a call costs cents) and now accepts the RESULT marker after a line of preface (Grok and Claude narrate first). Tested on September 24 with the five defaults: four said delete `lib/xml.ts`, Gemini Pro said keep it. Later on September 24 (owner) the critique skill was folded in: `/counsel critique` runs the five critique defaults (Sol, Astra, Gemini Pro, Gemini Flash, Grok) with the defect-hunting instruction; the runner moved to `~/.agents/skills/counsel/scripts/lanes.py`, and `providers.py` beside it now holds every model id and the Codex and Claude launch commands, imported by the flow's review lanes and planning pair. That fixed the pair calling Fable by the retired id `claude-fable-5`; Claude models are now named by alias (`opus`, `fable`, `sonnet`), which Claude's CLI resolves to the newest release. Terra was removed from every lane (owner), so QC runs the same eight runner lanes as critique, plus Opus when Claude Code hosts. Tested live: an Astra lane, a Sonnet lane and a pair-style Fable call. A `payments` bundle (Stripe best practices and docs) joined the feature table.
13. **The feature-flow hook, Astra as the only build model, Railway out** (September 24): see decisions.md; all committed on `beta`.
14. **Design sync done** (September 24): the design-system project "Oparax" in Claude Design holds the bundle, re-synced after #148's token trim.

## 2. Agreed in direction, waiting on the owner

1. **The design system is rebuilt on Mira; the owner re-syncs it (September 24).** Stock shadcn in the Mira style, installed for real on September 24 (the earlier pass applied only the theme and fonts, so the components had stayed Nova and the code `bzonVbKXA` recorded then was wrong); the owner's preset is `bzq0WEyKe`. Zinc base, lucide icons (hugeicons removed), the old Oparax blue tuned per mode for contrast, Nunito Sans headings, Source Sans 3 text, JetBrains Mono for handles and counts, dark by default with a light switch, 16px side gutters. Components stay stock (button, dialog, input, label, sonner, spinner, textarea, tooltip); controls use Mira's defaults with no added depth or focus rules. Every public page shares the header and a footer with Privacy, Terms and Contact; the Contact dialog is UI only until sending is built. DESIGN.md is the contract and `design-system/` (tokens.css, preview cards, README) is updated to match. **Waiting on:** the owner runs `/design-sync` so Claude Design gets the rebuilt system. The repo stays the source of truth; when the look changes, re-export and re-sync.
2. **Tests and alerts as one loop** (assistant's proposal; the owner said this thread was dropped and asked to keep it). A few unit tests on pure functions where a silent mistake costs money, security or the algorithm's rules (the X handle format, allowed return paths after login, "an unknown cost stays unknown", the Jev cutoff lines, the quote check on cards), run by build's existing gates with Node's built-in test runner; one PostHog alert ("error issue created or reopened") to a Slack channel; every bug fix adds one test that reproduces the bug; no end-to-end browser suites while the pages change. Vercel's verification skill is not a build step; at most a tracing checklist inside a bug fix. **Waiting on:** his yes (the proof bar rules out test suites), and the PostHog Slack channel (his click, or "set it up").
3. **The principles wording.** In AGENTS.md now; he may edit any line. The "about 400 lines" file limit is the assistant's number.

## 3. Open rulings, by when they are needed

1. **Before issue 1:** nothing blocks it; the owner's yes on tests and alerts and the PostHog Slack channel are still open but not blocking.
2. **Before issue 2:** the article fit line (keep 0.75 on and 0.35 off, lower it, or count the unsure band as on; the lab put 8 of Nihan's 25 items in that band, several plainly relevant); what Jev is told about the person (beat only, plus Grok's paragraph, plus the person's posts at about 25 cents a person a month; a re-read of Liam's and Nihan's posts costs about 10 cents each); which writer (Qwen 3.7 Flash copies quotes most cleanly, Ling produced more cards at no cost; the lab rates are automated acceptance, not truth); what a new monitor shows on day zero.
3. **In their issues:** where polling runs and how often (issue 2; a Vercel Pro cron every minute is the assistant's recommendation); how watched X accounts arrive (issue 5; polling looks best; three assistant recommendations exist and none is a decision); Stripe direct account or marketplace, and the Stripe plugin install (issue 5); the price (issue 5; none is decided); email and Slack as alert channels (after the slices, the owner's lean); the bot handle (the owner creates the plain account under farzan@oparax.ai and tries for @oparax before issue 5); per-service budgets (set after real usage, owner's rule; Vercel's budget does not cover the other services).

## 4. What the owner has said he does not understand, and how to answer it

1. **The onboarding algorithm.** He has read it several times and still does not hold it. Explain it as four steps with what goes in and what comes out, never as prompts (the verbatim prompts are in onboarding-algorithm.md). Slice 1's building page shows each step's input and output live; that is when it will land.
2. **The downstream algorithm.** Discuss it one step at a time in chat before slice 2, and write each ruling into downstream-algorithm.md with his name and date. Every R-numbered rule there is the assistant's proposal until it carries his date. Grouping is out of issue 2 entirely.
3. **Agent or pipeline.** Onboarding step 3 is a real tool-loop agent (Grok, six-step cap, two tools). The downstream is a fixed pipeline, and should be: each step gives one score, and a loop re-reads its whole context every turn. "Every desk is an agent" is a product sentence, not a design; in this repo "agent" already means one monitor row. eve is not coming back.
4. **Design surfaces.** Two, not four: the repo (where the look lives, applied from a shadcn preset code) and Claude Design (a synced copy to design with). The Claude app's Settings, Design systems, is the same list as Claude Design's. ui.shadcn.com/create is only the picker that produces the preset code. Both lists are empty because nothing has been synced yet; sync after the reset.
5. **Which doc is for what.** Answered by the consolidation (section 1, item 9); AGENTS.md's records line is the map.
6. **Testing, verification and PostHog together.** One loop (section 2, item 2). No browser-walking verify step (owner, September 23, after the four retirement reasons: screenshots before load, blind to runtime errors, port clash, missed handoffs between pages). The screenshot check is the small thing that survived.
7. **The skills and where they act.** The three-jobs rule and the ui bundle table are the answer; reviewers now read the same skill files.
8. **The whole project in one view.** He asked twice for the entire plan and every feature explained together and said the pieces are too big to take one by one. The September 22 "Oparax, start to finish" message in the chat he rewinds to is that explanation; the roadmap's build order (section 13) is its skeleton. When explaining again, keep that shape: product, money, accounts, onboarding, downstream, agent or pipeline, X delivery, Stripe, tooling, waste, what was invented, path.
9. **His own pattern.** He has said he over-builds meta tooling; the flow has been rewritten at least eight times since February. The flow is now frozen until issue 1 ships; the only edits left are the ones in section 2.
10. **The project as five issues, not nine.** When he asks how the whole thing goes, answer in his own five: onboarding, the feed page with its algorithm, the landing page and the whole flow, sign-up, and after sign-up. Nine slices is how it read before September 23 and confused him.

## 5. The order from here

1. `/feature 143` in a fresh chat, on the cleared `beta`: talk-through (this is where he finally watches the onboarding algorithm explained as four steps with inputs and outputs), design brief, Claude Design, plan, critique (eight lanes plus Opus, all reading the skill files and the principles), build, QC with the screenshot check, the owner's walk, ship.
3. The three downstream rulings, one step at a time in chat; then issue 2 (the feed page and its algorithm); then issue 3 (the landing page and the whole flow), issue 4 (sign-up), issue 5 (after sign-up), in that order.
