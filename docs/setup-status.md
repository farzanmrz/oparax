# Setup status

Saved at the owner's end-of-night pause, September 11, 2026 (Pacific). This is the operational baseline for the fresh experiment. The replacement product has not been built or launched. Resume from this record rather than assuming the admin cleanup or bot setup is finished.

## Start here next session

When the owner says "let's continue" or "what were we doing", first explain the completed baseline, the unresolved items and the next concrete step in plain language. Read this file, [setup-x.md](setup-x.md) and [exp1.md](exp1.md). Consult the Basic Memory note [Oparax Experiment 1 - Overnight handoff](https://app.basicmemory.com/notes?id=cloud%7C23ced63c-d463-4082-b0a4-646556de6c41%7C25f5c0c5-9dcd-4ea1-bc92-3830a613eece%7C77bc5055-51b0-4c90-b0b6-b927be7f4e26&explorer=project%3Acloud%7C23ced63c-d463-4082-b0a4-646556de6c41%7C25f5c0c5-9dcd-4ea1-bc92-3830a613eece) and its linked credential index. Do not ask the owner to reconstruct the conversation.

**Latest correction: the bot itself must appear as `@oparax`.** The owner rejected treating a separate company profile pointing to `@oparax_bot` as the solution. `@oparax_bot` is the existing provisioned identity, not an accepted final branding choice. Do not present that naming task as finished. The last turn only reopened the developer app; no bot edit, rename, replacement, transfer or new account creation followed before the owner paused for the night.

### Resume in this order

1. **Resolve the bot's identity.** Inspect its Edit control and the documented bot-management update/username behavior. Establish whether the existing bot can acquire or be renamed to `@oparax`, and whether Marketplace can transfer a handle to a project bot. If it cannot, investigate a regular `@oparax` account automated through the supported messaging API as an alternative mechanism for the same sender identity. Do not assert that a newly registered account can attach to the existing bot. Preserve existing keys/identity until a replacement is justified; do not delete the bot merely to free a slot.
2. **Finish the bounded admin audit.** The concrete leftovers are listed below. Report kept/removed/pending separately, then establish which are necessary before feature planning and which need the new code. Do not turn every available platform feature into setup work.
3. **Orient the experiment once.** Explain the learning, measurement and first build from the current draft, identify the remaining proposed choices, then let the owner trigger a fresh Claude Code `/feature` from `beta`. No feature workflow has been started. Do not reopen the three-week hypothesis debate or resume #131.
4. **Build and verify the new journey through that workflow.** Schema replacement, obsolete code removal, instrumentation, payment and bot runtime belong to the new plan. Restore production only after the replacement is ready and the owner chooses launch.

## What the experiment currently means

Vision: **monitor the world for each person**. The first value test targets people who regularly publish AI developments/tools/practical AI content on X. Liam and Nihan are initial cases, not customers already validated. Farzan and Kush are founder/personal-use comparators; Reshad is a football comparator at the offered cadence, not a demand for breaking-news infrastructure. Enterprises remain a separate discovery direction with no validated requirements.

The leap is that personalized news delivery improves the creator's existing routine enough to earn repeated use and payment. The two hypotheses test useful coverage/reduced discovery work, then payment at viable provision cost. X activity is evidence to reconcile with the person's explicit request, not permission to invent beliefs or treat every follow as an interest. The information may come from X, websites, RSS or other useful sources. Measure unique coverage before optimizing redundant paid acquisition; building cheaply with AI does not make adoption or payment evidence automatic.

The first engineering slice is Grok 4.6 through Vercel Gateway plus bounded X API evidence on **Liam, Nihan, Farzan, Kush and Reshad**, then iterating the inferred monitoring brief and source recommendations before connecting the broader feed. Website/RSS, GitHub and Product Hunt possibilities remain in exp1.md; they are not reasons to implement every adapter before seeing the first algorithm output. Qwen is a downstream candidate, not a settled replacement requirement. Founder-prepared examples do not validate automatic onboarding or willingness to pay.

The current **draft operating choices**, to confirm rather than present as already implemented:

- Readout: September 30, 2026, 23:59 Los Angeles. Seven days from signup plus 48 hours for payment observation; September 21 is the latest signup for a full observation window. September bounds measurement, not what can be built. Personal stakes: no traction by the end of September means seeking a job.
- At least ten eligible creators offered the same priced service and five complete signup windows; continue testing if three demonstrate repeated benefit and two of those pay. These are proposed pilot decision rules, not PMF benchmarks.
- Repeated benefit: meaningful use on three trial days including one of the final two, plus two concrete unique-coverage examples or two comparable sessions with at least 25% less discovery effort and no less usable information. The effort route requires pre-recorded baselines. Keep acquisition and assisted/self-setup results separate; incomplete trials and broken delivery are visible.
- Offer: free bounded handle preview; signup to connect/customize; seven-day no-card trial, then stop new personalized processing and DMs without payment, retaining old results. Proposed $29/month, one desk, hourly feed refresh, optional hourly/four-hourly/daily DM brief, ten pinned sources. Bot delivery defaults daily in the draft. No empty or unchanged repeats.
- Proposed ceiling $500: $200 ads, $250 operation, $50 reserve. Target recurring provision below $10/user/month, counting retrieval, models, delivery and allocated infrastructure, plus setup/acquisition separately. Unknown costs remain unknown. These draft allocations do not authorize Ads actions or a new paid plan. Standard account-DM price examples are not verified X Chat bot tariffs.
- PostHog measures entry/signup, deliberate story use/feedback, preference changes, optional bot subscriptions, notification attempts/results/interactions, trial/payment/refund/cancellation, monitoring failures and model traces/costs. Interviews provide concrete benefit evidence. Neither sent DMs nor an AI judge prove customer value. Exact events and denominators remain in exp1.md.

The [competition memo](research-monitoring.md) is saved. Alternatives validate that the job exists, not that Oparax wins. The trial must establish a useful advantage over participants' existing routines.

## Remaining admin and access work

- **Handle acquisition:** X's signed-in Marketplace listed `@oparax` as a Priority handle request. Personal use would replace `@farzanmrz` and is tied to Premium+; business use required Full Access Premium Business. Neither request was submitted. Whether this supports the project bot is unverified. An API lookup found no public `@oparaxAI` account, but normal registration availability is unverified. The owner has not accepted that as a fallback.
- **Account/subscription distinctions:** personal `@farzanmrz` Premium+ is active at $40/month. A separate normal company account can use `farzan@oparax.ai` if the address is not already attached to X; no new company account was created here. X supports account switching and X Pro multi-account management, not two active handles for one identity. Checked US web prices: Business Basic $200/month, Full Access $1,000/month, affiliate $50/month, before taxes. No Business plan or affiliate purchase is authorized. Owning a handle on a regular account does not establish that it can be transferred into an existing project bot.
- **Bot keys/runtime:** console registration succeeded and secrets are stored, but no owner send/reply has been tested. X documents Chat XDK and bot export_keys/import_keys storage. Resolve exporting/loading our console-created identity while retaining its registered key version. Do not deploy the PIN as a backend design by assumption or re-register keys to troubleshoot. Bot lifecycle app-bearer auth and runtime bot-token acceptance are separate checks. Incoming transport/webhook, opt-out, recipient binding, retry/deduplication and billing need implementation.
- **Supabase:** inspect actual schema, auth users, Vault/Edge Function secrets and integrations before deciding what remains relevant. This pass did not inventory or delete all of those. Deleting Workspace mailboxes did not establish deletion of their Supabase users. Review the quota warning before traffic; no upgrade purchased. Legacy API keys are disabled in apikey headers but remain valid JWTs, so do not call this complete signing-key revocation. Finish the user auth/OAuth journey after the new app is deployable.
- **Vercel and legacy provider cleanup:** old deployments/previews remain and may retain their original configuration; production alone is paused. Decide whether to delete obsolete previews/deployments while preserving project/domains. Removing environment variables did not revoke every underlying third-party provider key. Old direct-xAI, Bright Data/Exa, Slack app credentials, any BYOK providers, remaining integrations, subscriptions and billing commitments have not all been exhaustively audited or canceled. Retain only justified dependencies, but don't claim these accounts deleted.
- **Slack/Sentry:** two old channels archived and Vercel integration removed. Slack workspace/apps, existing PostHog connection and Slack Pro billing remain. Evaluate them, particularly whether the existing PostHog integration is useful, without automatically making Slack the alert dependency. The owner reports deleting Sentry's organization; the UI showed organization creation and no organization was recreated. No complete provider-account deletion audit was performed.
- **PostHog:** new token is configured, but no new experiment dashboard, alerts, replay/AI evaluation policy, warehouse/Stripe/X Ads integration or delivery/cost checks are implemented. Use its native capabilities meaningfully, with Vercel and Supabase diagnostics available. Instrument failures and overdue work, not only success events. Choose one owner alert destination after the actual signals are defined.
- **Stripe and X Ads:** Stripe merchant access, price, checkout, webhook and entitlements are pending the feature. Existing X Ads MCP entries/project preparation do not prove authenticated integration. The owner explicitly prohibited any Ads setup, launch or campaign change during cleanup. Leave Ads alone until separately instructed; preserve the distinction from the proposed future recruitment plan.
- **Broader personal presence:** personal website and X/LinkedIn publishing cadence were discussed but not scoped or built. Personal Premium is the only completed subscription step for that work. Do not silently fold that work into the experiment feature.

## Working preferences and authorizations

The owner wants sequential browser work, concise updates, no tables, no em dashes and plain language. Chrome was explicitly authorized for setup after an earlier prohibition; prior Chrome restrictions should not be treated as current. Re-inspect tabs at resume because IDs and login state can change. Use Spark for email/calendar. Do not send customer/third-party messages, launch Ads, buy Premium Business, rename the personal account or automatically start a feature workflow. Ordinary authorized cleanup does not need repetitive permission questions. If a specific irreversible action or new cost needs a decision, finish its investigation and present the concrete consequence first.

The owner has gone to sleep. No overnight automation or further platform mutations were requested. Future sessions should orient first, then proceed with the chosen task. The requirement is the sender **@oparax**, not a separate brand profile pointing at the current bot.

## Completed

- **Repository:** `beta` is the working baseline; `main` remains production. Old feature branches and issue #131 are retired. No open issues or pull requests remained at the cleanup check. The old product source remains until the replacement feature specifies its removal.
- **Railway:** deleted project `oparax-ingest` and both the website poller and X ingest services. Used immediate deletion after the initial grace-period delete. Dashboard showed zero projects. Retired local worker credentials were moved out of the active checkout after backup. This does not mean the Railway account subscription was canceled.
- **Vercel:** removed all old project environment variables, then installed the nine names in [setup-env.md](setup-env.md) across Production, Preview and Development. Pulled `.env.local` from Development. Removed the Slack integration. **Production is paused**, so `oparax.ai` serves Vercel's paused response until resumed. Project settings, domains, historical deployments and previews remain; pausing production does not delete or pause previews.
- **Supabase:** retained shared project `pcgvpypzfwuchyfwdlwe` and auth configuration. Created the new publishable/server key pair, removed old modern keys, and disabled legacy keys for `apikey` header use. Legacy JWT signing was not rotated. No new schema migration or auth-user deletion was performed in this credential cleanup.
- **Authentication email:** corrected the SMTP username to `farzan@oparax.ai`; sender remains **Oparax <no-reply@oparax.ai>**. A Supabase recovery email arrived in Spark. See [setup-smtp.md](setup-smtp.md). The full application signup/recovery journey remains to be exercised after the rebuild.
- **X:** rotated app bearer and OAuth 2 client secret, updated the app description/settings, removed the obsolete football/crypto streaming rule. The app has no webhooks or subscriptions and its latest stream connection is disconnected. **@oparax_bot** exists and its encryption keys are registered successfully. Token and PIN are saved. This remains a placeholder because the owner requires the bot handle **@oparax**. See [setup-x.md](setup-x.md).
- **PostHog:** retained project `563049`, rotated its project token and installed it in the app environments. Existing history remains. The new experiment dashboard, failure alerts and AI instrumentation are not implemented by a token reset.
- **Slack/Sentry:** archived the obsolete `engineering-quality` and `release-health` channels and verified their read-only archive state. Removed Vercel's Slack integration and app Slack/Sentry variables. Sentry was not recreated. The Slack workspace, its installed apps and the existing PostHog integration were not deleted; Slack is not a required runtime service for the new experiment.
- **Credentials:** replacement Supabase, X API, X bot/PIN, Gateway and PostHog records are in Basic Memory, with a single **Oparax Experiment 1 - Credential Index** linking them. Values were read back privately. The Workspace login and SMTP password records remain separate owner-managed entries.
- **Browser:** closed seven completed or duplicate setup tabs. Kept the current core-service consoles and the owner's other tabs.

## Verified, within these limits

Fresh Supabase public-auth access, Supabase admin access, Gateway credit access and one X public user lookup returned HTTP 200. X confirmed chat-key registration. Spark confirmed SMTP arrival. These are credential/platform checks, not proof of a working feed, OAuth connection, chatbot delivery, payment flow or experiment.

## What the fresh feature must settle and build

1. **Learning and measurements:** use [exp1.md](exp1.md), reconcile its proposed operating choices once, and derive the build from its hypotheses and decision rules. The first algorithm check uses Liam, Nihan, Farzan, Kush and Reshad; founder-prepared examples are not customer validation.
2. **Replacement schema and repository cleanup:** inspect the actual shared schema, preserve needed auth, remove obsolete voice/drafting/posting flows and their tables/RPCs, and retain source-discovery or telemetry code only where it serves the new flow. The old deployment already showed schema drift, including missing `voice_guides` and `drafts.posted_at`. Do not try to repair the retired product as a prerequisite to planning the replacement. Preserve a recoverable database copy before destructive schema changes.
3. **First personalization algorithm:** Grok 4.6 through Vercel Gateway, using bounded X API evidence and the person's explicit monitoring request. Establish the output and evaluate recommendations before expanding acquisition to selected RSS, sites and other sources. Verify the model route with a bounded generation; credentials alone do not prove tool support.
4. **Complete user journey:** free handle preview, signup for connecting/customizing, seven-day trial and a real selected price, feed, optional bot delivery, corrections and opt-out. The bot's client/encryption protocol is documented by X; loading our existing console-created key identity, actual send/reply and tariffs still require integration proof. Stripe setup belongs with payment and webhook implementation.
5. **Monitoring:** use PostHog as the primary place to connect user behavior, errors, AI traces/evaluations, delivery results and costs using shared user/story/run IDs. Keep Vercel/Supabase native diagnostics for provider failures. A missing successful event needs a separate failure or overdue-work signal; a sent DM is not a read receipt, and an LLM judge is not customer validation. Select the owner's alert destination after the actual alerts exist, without making Slack a runtime dependency.
6. **Runtime and launch:** choose scheduling from measured task duration, retry and cadence needs. Do not recreate Railway automatically. Confirm Supabase quota, the priced trial and full acceptance journey, deploy the replacement, then resume Vercel production. The owner controls the launch.

## Explicitly outside this cleanup

No ad campaign or X Ads setup, no new payment subscription, no customer DM, no automatic feature/build launch, and no replacement database schema. The owner has independently activated personal X Premium at $40/month. Historical Vercel deployments and Slack app installations remain as stated above; they have not been represented as deleted.

## Records and recovery

[setup.md](setup.md) is the short handoff index. Provider details are in [setup-x.md](setup-x.md), [setup-env.md](setup-env.md) and [setup-smtp.md](setup-smtp.md). The source of truth for the target experiment remains [exp1.md](exp1.md).

Private configuration recovery: `/Users/farzanm4/Desktop/oparax-recovery/clean-reset-20260912-014947`. Earlier repository/issue/planning recovery: `/Users/farzanm4/Desktop/oparax-recovery/20260911-173343`. Neither is a specification or a source from which to restore obsolete secrets wholesale.
