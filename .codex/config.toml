# Project-scoped Codex config for oparax.

model_verbosity = "medium"
web_search = "indexed"
approval_policy = "on-request"
# $build commits on ft/<N>; the default workspace-write sandbox keeps .git read-only
# (owner decision 2026-08-17: the build session gets full access).
sandbox_mode = "danger-full-access"
