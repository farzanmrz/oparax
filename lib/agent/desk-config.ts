// lib/agent/desk-config.ts
//
// Platform constants shared by the story pipeline and the feed. The X character-ceiling
// machinery (tiers, postability gates) died with drafting/posting.

/** X is the only platform the product lands stories for. */
export type Platform = "x";
