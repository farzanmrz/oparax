interface:
  display_name: "STAT"
  short_description: "Show deterministic outreach totals"
  default_prompt: "Use $stat to show the current Oparax outreach status."
policy:
  allow_implicit_invocation: false
