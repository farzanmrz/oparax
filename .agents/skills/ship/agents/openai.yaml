interface:
  display_name: "ship"
  short_description: "Ship an oparax ft/<N> branch to beta after QC"
  default_prompt: "Use $ship <N> to squash the finished ft/<N> branch to beta after the owner's localhost walk."

policy:
  allow_implicit_invocation: false
